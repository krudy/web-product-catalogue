
<div class="d-flex justify-content-center mb-3">
    <% if(user && user.firstName) { %>
        <p class="fw-bold fs-4">Witaj <%- user.firstName %></p>
    <% } %>
</div>

<div class="container">
    <div class="row">
        <div class="col-12 d-flex justify-content-center">
            <img src="/img/top_baner.png" alt="Opis obrazka" class="img-fluid">
        </div>
    </div>
</div>